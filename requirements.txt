# requirements.txt — pinned for reproducibility
# Note: install the appropriate torch wheel for your CUDA / CPU platform:
#   e.g., pip install torch --index-url https://download.pytorch.org/whl/cu117

numpy==1.25.2
scipy==1.11.3
pandas==2.2.2
scikit-learn==1.3.3

# PyTorch (choose appropriate build for CUDA/CPU as noted above)
torch>=2.0,<3.0
torchvision>=0.15,<1.0
torchaudio>=2.0,<3.0

# Configuration, logging, and experiment management
pyyaml==6.0
omegaconf==2.3.0
hydra-core==1.3.2
tensorboard==2.15.0
accelerate==0.20.3
wandb==0.15.11

# Utilities and dev tooling
tqdm==4.66.1
pytest==7.4.2
black==24.1.0
isort==5.12.0